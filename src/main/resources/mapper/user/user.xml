<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.healthcare.timemanagerapi.mapper.user.account.UserAccountMapper">

    <select id="findByStudentNumberCount" parameterType="String" resultType="int">
        SELECT COUNT(*)
        FROM tbl_application
        WHERE STUDENT_NUMBER = #{value}
    </select>

    <select id="findByStudentIdCount" parameterType="String" resultType="int">
        SELECT COUNT(*)
        FROM tbl_application
        WHERE MEMBER_ID = #{value}
    </select>

    <insert id="persistByStudentAccount" parameterType="kr.healthcare.timemanagerapi.domain.user.account.UserAccountVO">
        INSERT INTO tbl_application(STUDENT_NUMBER,MEMBER_ID,MEMBER_PW,MEMBER_GENDER,MEMBER_NAME, SALT)
        VALUES(#{studentNumber},#{memberId},#{memberPw},#{memberGender},#{memberName},#{salt})
    </insert>


</mapper>